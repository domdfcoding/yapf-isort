# This file is managed by 'repo_helper'. Don't edit it directly.
---

os: linux
language: python
dist: focal
cache: pip
python:
  - '3.6'

arch:
  - arm64
install:

  - pip install --upgrade pip
  - pip install tox tox-travis
  - pip install coverage_pyver_pragma

script:
  - tox -vv

stages:
  - test
